/**
 * Pipes Standalone Reutilizables
 * 
 * @description
 * Este directorio contiene pipes Angular standalone que transforman datos
 * antes de mostrarlos en las plantillas HTML (ej. formatear fechas, monedas, filtrar listas).
 * Estos pipes están diseñados para ser reutilizados en toda la aplicación.
 * 
 * @usageNotes
 * Los pipes standalone deben ser importados en el array imports de los
 * componentes standalone que los necesiten.
 */